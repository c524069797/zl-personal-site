# 文章自动发布工作流

## 🎯 功能说明

当你在 `content/posts/` 目录下新增或修改文章后，可以使用此工作流自动发布到数据库。

## 🚀 快速使用

### 方法一：使用 npm 脚本（推荐）

```bash
# 发布所有新文章
npm run publish:post

# 自动发布（忽略 draft 标记）
npm run publish:post-auto
```

### 方法二：直接运行脚本

```bash
# 发布所有新文章
npx tsx scripts/auto-publish-post.ts

# 发布单个文件
npx tsx scripts/auto-publish-post.ts content/posts/my-article.md

# 自动发布（忽略 draft）
npx tsx scripts/auto-publish-post.ts --auto-publish
```

### 方法三：在 Cursor Composer 中使用

直接在 Cursor Composer 中输入：

```
我新增了一篇文章，请帮我自动发布
```

Cursor AI 会自动识别并执行发布流程。

## 📝 文章格式要求

确保你的 Markdown 文件包含以下 frontmatter：

```yaml
---
title: "文章标题"           # 必需
date: "2025-01-20"          # 必需
summary: "文章摘要"         # 可选
tags: ["标签1", "标签2"]    # 可选
category: "tech"            # 可选：tech 或 life
draft: false                # 可选：false=发布，true=草稿
---
```

## ✨ 特性

- ✅ 自动分类（tech/life）
- ✅ 自动创建/更新标签
- ✅ 跳过已存在的文章
- ✅ 支持批量处理
- ✅ 详细的错误处理和日志
- ✅ 非交互式，适合自动化

## 📚 更多信息

查看完整文档：`docs/deployment/CURSOR_WORKFLOW.md`



